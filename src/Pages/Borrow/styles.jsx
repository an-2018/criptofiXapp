import styled from 'styled-components';

export const Header = styled.div`
  grid-area: B;

  font-size: 1.75rem;
  display: flex;
  flex-direction:row;
  align-items: center;
  justify-content: flex-end;
`;
 